# Final Project: Bike Sharing Dataset(*Hourly basis*)

# Module Requirement
`conda create --name main python=3.9`
`conda activate main`
`pip install -r requirements.txt`

# Run Streamlit App
`streamlit run dashboard.py`
